<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>
<body>
    <h1>Home</h1>
     <%- include('./partials/nav.ejs') %>

    <div>
        <% if (blogs.length > 0) {%>
            <% blogs.forEach(blog => { %>
                <h3><%= blog.title%></h3>
                <p><%= blog.snippet%></p>
            <% }) %>
        <% } else { %>
            <p>there are no blogs to display...</p>
        <% } %>
    </div>
</body>
</html>